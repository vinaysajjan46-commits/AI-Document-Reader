# AI-Document-Reader
AI-powered document reader that extracts, summarizes, and analyzes information from documents, making complex content easier to understand and search.
simple and interactive interface.
inside terminal write python -m venv venv
next type venv\scripts\activate
# Liberries 
pip install streamlit 
pip install langchain
pip install langchain-community
pip install langchain-google-genai
pip install google-generativeai
pip install faiss-cpu
pip install pypdf
pip install python-docx
pip install senetence-transformers
pip install python-dotenv
